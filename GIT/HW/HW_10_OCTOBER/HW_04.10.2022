для полной очистки индекса   git stash . 
для просмотра разницы между раб.каталогом и посл. коммитом  git diff HEAD
для создания новой ветки и одновременного переключения в на нее   git checkout -b newbranchname
для сравнения веток между собой  git diff main develop  
для удаления локальной ветки  git branch -d branchname
для слияния веток   переключится на главную ветку с которой будем мержить (напр main) затем   git merge develop
для перезаписи посл/коммита  git --amend -m "имя коммита который перезапишет посл коммит"

Коротко опишите причину возникновения конфликтов при слиянии и порядок действий для их решения.

самая распостранненая причина в одном и том же файле в разных ветках заполнена одна и та же строчка 
после git merge получим уведомление о конфликте
Поетому заходим в редактор и оставляем нужный изменения и удаляем ненужные 
затем сохраняем , коммитим и можно ужалять ненужную ветку  
